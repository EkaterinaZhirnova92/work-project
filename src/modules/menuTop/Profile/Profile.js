
function Profile() {
    return (
        <div className="workspace">
            This is the interface of module One.<br/>
            One does not introduce internal routes.
        </div>
    );
}

export default Profile;
